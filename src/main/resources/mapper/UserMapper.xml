<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kdt.taskflow.mapper.UserMapper">

    <resultMap id="UserResult" type="com.kdt.taskflow.domain.User">
        <id     property="id"        column="id"/>
        <result property="username"  column="username"/>
        <result property="email"     column="email"/>
        <result property="password"  column="password"/>
        <result property="fullName"  column="full_name"/>
        <result property="role"      column="role"/>
        <result property="status"    column="status"/>
        <result property="deleted"   column="deleted"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="columns">
        id, username, email, password, full_name, role, status, deleted, created_at, updated_at
    </sql>

    <insert id="insert" parameterType="com.kdt.taskflow.domain.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO app_user (username, email, password, full_name, role, status, deleted)
        VALUES (#{username}, #{email}, #{password}, #{fullName}, #{role}, #{status}, false)
    </insert>

    <select id="findById" resultMap="UserResult">
        SELECT <include refid="columns"/>
        FROM app_user
        WHERE id = #{id}
    </select>

    <select id="findByUsername" resultMap="UserResult">
        SELECT <include refid="columns"/>
        FROM app_user
        WHERE username = #{username} AND deleted = false
    </select>

    <select id="findByEmail" resultMap="UserResult">
        SELECT <include refid="columns"/>
        FROM app_user
        WHERE email = #{email} AND deleted = false
    </select>

    <select id="search" resultMap="UserResult">
        SELECT <include refid="columns"/>
        FROM app_user
        <where>
            AND deleted = false
            <if test="role != null">
                AND role = #{role}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (username ILIKE '%' || #{keyword} || '%'
                OR email ILIKE '%' || #{keyword} || '%'
                OR full_name ILIKE '%' || #{keyword} || '%')
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

    <select id="searchAdmin" resultMap="UserResult">
        SELECT <include refid="columns"/>
        FROM app_user
        <where>
            <if test="username != null and username != ''">
                AND username ILIKE '%' || #{username} || '%'
            </if>
            <if test="email != null and email != ''">
                AND email ILIKE '%' || #{email} || '%'
            </if>
            <if test="role != null">
                AND role = #{role}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

    <update id="update" parameterType="com.kdt.taskflow.domain.User">
        UPDATE app_user
        SET username   = #{username},
            email      = #{email},
            password   = #{password},
            full_name  = #{fullName},
            role       = #{role},
            status     = #{status},
            updated_at = now()
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE app_user
        SET password   = #{password},
            updated_at = now()
        WHERE id = #{id}
    </update>

    <update id="softDeleteById">
        UPDATE app_user
        SET deleted    = true,
            status     = 'INACTIVE',
            updated_at = now()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM app_user WHERE id = #{id}
    </delete>

</mapper>
